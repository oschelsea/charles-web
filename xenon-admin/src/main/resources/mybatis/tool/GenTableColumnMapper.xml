<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.xenon.project.tool.gen.mapper.GenTableColumnMapper">

    <resultMap type="GenTableColumn" id="GenTableColumnResult">
        <id property="columnId" column="column_id"/>
        <result property="tableId" column="table_id"/>
        <result property="columnName" column="column_name"/>
        <result property="columnComment" column="column_comment"/>
        <result property="columnType" column="column_type"/>
        <result property="javaType" column="java_type"/>
        <result property="javaField" column="java_field"/>
        <result property="isPk" column="is_pk"/>
        <result property="isIncrement" column="is_increment"/>
        <result property="isRequired" column="is_required"/>
        <result property="isInsert" column="is_insert"/>
        <result property="isEdit" column="is_edit"/>
        <result property="isList" column="is_list"/>
        <result property="isQuery" column="is_query"/>
        <result property="queryType" column="query_type"/>
        <result property="htmlType" column="html_type"/>
        <result property="dictType" column="dict_type"/>
        <result property="sort" column="sort"/>
        <result property="createBy" column="create_by"/>
        <result property="createTime" column="create_time"/>
        <result property="updateBy" column="update_by"/>
        <result property="updateTime" column="update_time"/>
    </resultMap>

    <resultMap type="GenTableColumn" id="DbTableColumnResult">
        <result property="columnName" column="name"/>
        <result property="columnType" column="type"/>
        <result property="isPk" column="pk"/>
        <result property="isRequired" column="notnull"/>
    </resultMap>

    <!-- PostgreSQL 列元数据结果映射 -->
    <resultMap type="GenTableColumn" id="DbTableColumnResultPg">
        <result property="columnName" column="column_name"/>
        <result property="columnComment" column="column_comment"/>
        <result property="columnType" column="column_type"/>
        <result property="isPk" column="is_pk"/>
        <result property="isRequired" column="is_required"/>
        <result property="isIncrement" column="is_increment"/>
    </resultMap>

    <sql id="selectGenTableColumnVo">
        select column_id, table_id, column_name, column_comment, column_type, java_type, java_field, is_pk, is_increment, is_required, is_insert, is_edit, is_list, is_query, query_type, html_type, dict_type, sort, create_by, create_time, update_by, update_time from gen_table_column
    </sql>

    <!-- selectGenTableColumnListByTableId moved to default method in Java interface -->

    <!-- SQLite 查询表列信息 -->
    <select id="selectDbTableColumnsByName" parameterType="String" resultMap="DbTableColumnResult" databaseId="sqlite">
        <!-- 表名必须使用 $ -->
        PRAGMA table_info(${tableName})
	</select>

    <!-- PostgreSQL 查询表列信息 -->
    <select id="selectDbTableColumnsByName" parameterType="String" resultMap="DbTableColumnResultPg" databaseId="postgresql">
        SELECT
            c.column_name,
            col_description((table_schema || '.' || table_name)::regclass, c.ordinal_position) AS column_comment,
            c.udt_name AS column_type,
            CASE WHEN pk.column_name IS NOT NULL THEN '1' ELSE '0' END AS is_pk,
            CASE WHEN c.is_nullable = 'NO' THEN '1' ELSE '0' END AS is_required,
            CASE WHEN c.column_default LIKE 'nextval%' THEN '1' ELSE '0' END AS is_increment
        FROM information_schema.columns c
        LEFT JOIN (
            SELECT kcu.column_name
            FROM information_schema.table_constraints tc
            JOIN information_schema.key_column_usage kcu
                ON tc.constraint_name = kcu.constraint_name
                AND tc.table_schema = kcu.table_schema
            WHERE tc.constraint_type = 'PRIMARY KEY'
            AND tc.table_schema = 'public'
            AND tc.table_name = #{tableName}
        ) pk ON c.column_name = pk.column_name
        WHERE c.table_schema = 'public'
        AND c.table_name = #{tableName}
        ORDER BY c.ordinal_position
    </select>


</mapper>
